#!/bin/sh
# Rede de seguranca antes do push: tipos + testes do motor fiscal.
# Emergencia (pula a checagem): git push --no-verify
set -e
echo "pre-push -> pnpm typecheck"
pnpm typecheck
echo "pre-push -> pnpm test"
pnpm test
echo "pre-push OK"
